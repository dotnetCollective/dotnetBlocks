<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RepositoryType>git</RepositoryType>

    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>

    <PackageDescription> Contracts and models for the DotNetBlocks event based actor model architecture.

Commonly Used Types:

    Operation
    BusinessEntity
    Keyed Entity
    AuditProperties
    IBusinessLogic
    IKeyedEntity
    GlobalKeyedEntity
    AuditableKeyedEntity
    AuditableGlobalKeyedEntity

</PackageDescription>

  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\dotNetBlocks.System\dotNetBlocks.System.csproj" />
  </ItemGroup>

</Project>
